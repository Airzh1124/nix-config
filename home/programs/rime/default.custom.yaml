# Rime Patch settings
# encoding: utf-8

patch:
  __include: rime_ice_suggestion:/
  __patch:
    schema_list:
      - schema: rime_ice

    ascii_composer:
      good_old_caps_lock: true
      switch_key:
        Caps_Lock: noop
